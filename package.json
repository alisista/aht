{
  "name": "aht",
  "version": "1.0.0",
  "dependencies": {
    "@waves/waves-api": "^1.4.0",
    "firebase": "^5.5.7",
    "gatsby": "next",
    "gatsby-plugin-react-helmet": "next",
    "moment-timezone": "^0.5.21",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-helmet": "^5.2.0",
    "shortid": "^2.2.14",
    "underscore": "^1.9.1",
    "uuid": "^3.3.2"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "deploy": "gatsby build && echo 'alishackers.club' > public/CNAME && gh-pages -d public -b master",
    "deploy-testnet": "gatsby build && echo 'testnet.alishackers.club' > public/CNAME && gh-pages -d public -o testnet",
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write '**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "prettier": "^1.14.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gatsbyjs/gatsby-starter-default"
  }
}
